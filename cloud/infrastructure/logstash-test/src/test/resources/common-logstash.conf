input {
  stdin {
    tags => [
      "corfu",
      "corfu-jvm"
    ]
    codec => multiline {
      pattern => "^%{TIMESTAMP_ISO8601}"
      negate => true
      what => "previous"
    }
  }
}

output {
  stdout { codec => rubydebug }
  file {
    path => "/logstash-test-output/output.log"
    codec => "json_lines"
  }
}